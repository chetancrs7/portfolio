## Question

How do embedding choices and HNSW settings affect retrieval quality and latency tradeoffs?

## Setup

This lab keeps the content deliberately compact. The goal is to verify that lab-style work can use the same MDX pipeline without becoming a full flagship case study.

## Experiment

Compare a small set of embedding and index settings against representative queries. Record quality and latency separately so speed does not hide retrieval misses.

## Results

<Callout type="note">
  Detailed benchmark values are not documented yet. This lab exists to test the
  shorter MDX authoring path without inventing measurements.
</Callout>

## Interpretation

Retrieval behavior should be evaluated as a tradeoff between result quality, index configuration, and serving cost.

## Limitations

<Limitations
  items={[
    "No finalized benchmark table is available yet.",
    "The query set and index settings still need documentation.",
  ]}
/>

## What I Learned

Short lab notes should stay lightweight, but they still benefit from explicit questions, setup, results, and limitations.
